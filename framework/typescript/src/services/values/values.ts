// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { BaseService } from '../base-service';
import { ContentType, HttpResponse, RequestConfig } from '../../http/types';
import { RequestBuilder } from '../../http/transport/request-builder';
import { SerializationStyle } from '../../http/serialization/base-serializer';
import {
  AddParams,
  AllParams,
  DisplayParams,
  Get_Params,
  New_Params,
  RemoveParams,
  Set_Params,
  UpdateParams,
} from './request-params';
import { Value, valueRequest, valueResponse } from '../common/value';

export class ValuesService extends BaseService {
  /**
   * Create new dataset (default or specified settings)
   * @param {string} [init] -
   * @param {string} [dataset] -
   * @returns {Promise<HttpResponse<any>>} Successful Response
   */
  async new_(params?: New_Params, requestConfig?: RequestConfig): Promise<HttpResponse<any>> {
    const request = new RequestBuilder<any>()
      .setBaseUrl(this.config)
      .setConfig(this.config)
      .setMethod('PUT')
      .setPath('/values/revision/new')
      .setRequestSchema(z.any())
      .setResponseSchema(z.any())
      .setRequestContentType(ContentType.Json)
      .setResponseContentType(ContentType.Json)
      .setRetryAttempts(this.config, requestConfig)
      .setRetryDelayMs(this.config, requestConfig)
      .setResponseValidation(this.config, requestConfig)
      .addQueryParam({
        key: 'init',
        value: params?.init,
      })
      .addQueryParam({
        key: 'dataset',
        value: params?.dataset,
      })
      .build();
    return this.client.call<any>(request);
  }

  /**
   * Obtain the lastest value for variable with specified 'name'
   * @param {string} name -
   * @param {string} [dataset] -
   * @returns {Promise<HttpResponse<string>>} Successful Response
   */
  async get_(params: Get_Params, requestConfig?: RequestConfig): Promise<HttpResponse<string>> {
    const request = new RequestBuilder<string>()
      .setBaseUrl(this.config)
      .setConfig(this.config)
      .setMethod('GET')
      .setPath('/values/actual/get')
      .setRequestSchema(z.any())
      .setResponseSchema(z.string())
      .setRequestContentType(ContentType.Json)
      .setResponseContentType(ContentType.Json)
      .setRetryAttempts(this.config, requestConfig)
      .setRetryDelayMs(this.config, requestConfig)
      .setResponseValidation(this.config, requestConfig)
      .addQueryParam({
        key: 'name',
        value: params?.name,
      })
      .addQueryParam({
        key: 'dataset',
        value: params?.dataset,
      })
      .build();
    return this.client.call<string>(request);
  }

  /**
   * Remove all previous values for specified 'name' and add a new value
   * @param {string} name -
   * @param {string} [value] -
   * @param {string} [dataset] -
   * @returns {Promise<HttpResponse<any>>} Successful Response
   */
  async set_(params: Set_Params, requestConfig?: RequestConfig): Promise<HttpResponse<any>> {
    const request = new RequestBuilder<any>()
      .setBaseUrl(this.config)
      .setConfig(this.config)
      .setMethod('PUT')
      .setPath('/values/actual/set')
      .setRequestSchema(z.any())
      .setResponseSchema(z.any())
      .setRequestContentType(ContentType.Json)
      .setResponseContentType(ContentType.Json)
      .setRetryAttempts(this.config, requestConfig)
      .setRetryDelayMs(this.config, requestConfig)
      .setResponseValidation(this.config, requestConfig)
      .addQueryParam({
        key: 'name',
        value: params?.name,
      })
      .addQueryParam({
        key: 'value',
        value: params?.value,
      })
      .addQueryParam({
        key: 'dataset',
        value: params?.dataset,
      })
      .build();
    return this.client.call<any>(request);
  }

  /**
   * Add a new value for specified 'name'
   * @param {string} name -
   * @param {string} [value] -
   * @param {string} [dataset] -
   * @returns {Promise<HttpResponse<any>>} Successful Response
   */
  async add(params: AddParams, requestConfig?: RequestConfig): Promise<HttpResponse<any>> {
    const request = new RequestBuilder<any>()
      .setBaseUrl(this.config)
      .setConfig(this.config)
      .setMethod('PUT')
      .setPath('/values/actual/add')
      .setRequestSchema(z.any())
      .setResponseSchema(z.any())
      .setRequestContentType(ContentType.Json)
      .setResponseContentType(ContentType.Json)
      .setRetryAttempts(this.config, requestConfig)
      .setRetryDelayMs(this.config, requestConfig)
      .setResponseValidation(this.config, requestConfig)
      .addQueryParam({
        key: 'name',
        value: params?.name,
      })
      .addQueryParam({
        key: 'value',
        value: params?.value,
      })
      .addQueryParam({
        key: 'dataset',
        value: params?.dataset,
      })
      .build();
    return this.client.call<any>(request);
  }

  /**
   * Obtain a list of all values with specified 'name'
   * @param {string} [name] -
   * @param {string} [dataset] -
   * @returns {Promise<HttpResponse<Value[]>>} Successful Response
   */
  async all(params?: AllParams, requestConfig?: RequestConfig): Promise<HttpResponse<Value[]>> {
    const request = new RequestBuilder<Value[]>()
      .setBaseUrl(this.config)
      .setConfig(this.config)
      .setMethod('GET')
      .setPath('/values/all/get')
      .setRequestSchema(z.any())
      .setResponseSchema(z.array(valueResponse))
      .setRequestContentType(ContentType.Json)
      .setResponseContentType(ContentType.Json)
      .setRetryAttempts(this.config, requestConfig)
      .setRetryDelayMs(this.config, requestConfig)
      .setResponseValidation(this.config, requestConfig)
      .addQueryParam({
        key: 'name',
        value: params?.name,
      })
      .addQueryParam({
        key: 'dataset',
        value: params?.dataset,
      })
      .build();
    return this.client.call<Value[]>(request);
  }

  /**
   * Remove previously set and add new values with specified 'name' fileds with values from 'values' fileds of provided list
   * @param {string} [dataset] -
   * @returns {Promise<HttpResponse<any>>} Successful Response
   */
  async update(body: Value[], params?: UpdateParams, requestConfig?: RequestConfig): Promise<HttpResponse<any>> {
    const request = new RequestBuilder<any>()
      .setBaseUrl(this.config)
      .setConfig(this.config)
      .setMethod('PUT')
      .setPath('/values/all/update')
      .setRequestSchema(z.any())
      .setResponseSchema(z.any())
      .setRequestContentType(ContentType.Json)
      .setResponseContentType(ContentType.Json)
      .setRetryAttempts(this.config, requestConfig)
      .setRetryDelayMs(this.config, requestConfig)
      .setResponseValidation(this.config, requestConfig)
      .addQueryParam({
        key: 'dataset',
        value: params?.dataset,
      })
      .addHeaderParam({ key: 'Content-Type', value: 'application/json' })
      .addBody(body)
      .build();
    return this.client.call<any>(request);
  }

  /**
   * Remove all values for specified 'name'
   * @param {string} [name] -
   * @param {string} [value] -
   * @param {string} [dataset] -
   * @returns {Promise<HttpResponse<any>>} Successful Response
   */
  async remove(params?: RemoveParams, requestConfig?: RequestConfig): Promise<HttpResponse<any>> {
    const request = new RequestBuilder<any>()
      .setBaseUrl(this.config)
      .setConfig(this.config)
      .setMethod('DELETE')
      .setPath('/values/all/remove')
      .setRequestSchema(z.any())
      .setResponseSchema(z.any())
      .setRequestContentType(ContentType.Json)
      .setResponseContentType(ContentType.Json)
      .setRetryAttempts(this.config, requestConfig)
      .setRetryDelayMs(this.config, requestConfig)
      .setResponseValidation(this.config, requestConfig)
      .addQueryParam({
        key: 'name',
        value: params?.name,
      })
      .addQueryParam({
        key: 'value',
        value: params?.value,
      })
      .addQueryParam({
        key: 'dataset',
        value: params?.dataset,
      })
      .build();
    return this.client.call<any>(request);
  }

  /**
   * Display a list of all values with specified 'name'
   * @param {string} [name] -
   * @param {string} [dataset] -
   * @param {string} [format] -
   * @returns {Promise<HttpResponse<any>>} Successful Response
   */
  async display(params?: DisplayParams, requestConfig?: RequestConfig): Promise<HttpResponse<any>> {
    const request = new RequestBuilder<any>()
      .setBaseUrl(this.config)
      .setConfig(this.config)
      .setMethod('GET')
      .setPath('/values/all/display')
      .setRequestSchema(z.any())
      .setResponseSchema(z.any())
      .setRequestContentType(ContentType.Json)
      .setResponseContentType(ContentType.Json)
      .setRetryAttempts(this.config, requestConfig)
      .setRetryDelayMs(this.config, requestConfig)
      .setResponseValidation(this.config, requestConfig)
      .addQueryParam({
        key: 'name',
        value: params?.name,
      })
      .addQueryParam({
        key: 'dataset',
        value: params?.dataset,
      })
      .addQueryParam({
        key: 'format',
        value: params?.format,
      })
      .build();
    return this.client.call<any>(request);
  }
}
